NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=anon-key

# Optional: bucket name used by ImageUploader
NEXT_PUBLIC_SUPABASE_IMAGE_BUCKET=resort-images
NEXT_PUBLIC_ENABLE_PHONE_AUTH=false

# Note: Do NOT include service role keys in client env files.
# Configure any server-only secrets (e.g., SUPABASE_SERVICE_ROLE_KEY) in your hosting
# provider's server environment (e.g., Vercel Project Settings → Environment Variables).
