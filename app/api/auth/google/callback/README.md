# Google OAuth Callback (Disabled)

This API route is intentionally left unused. Supabase handles provider callbacks and session creation client-side.

If you later migrate to a server-side callback, add a `route.ts` here to verify the provider response and create the session.
