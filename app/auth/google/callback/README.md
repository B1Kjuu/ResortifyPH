# Google OAuth Callback Page (Disabled)

This route folder is present but unused. Supabase's client-side OAuth flow returns to the app root; no page-level callback is required.

If you later need to render a post-callback UI, add a `page.tsx` here to read query params and display status.
